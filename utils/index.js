// const connection = require('../config/connection');
const {User, Thoughs} = require("../models");

const thoughSeeds = require('./thoughSeed.json');
const userSeeds= require('./userSeed.json')
console.log(thoughSeeds)
console.log(userSeeds)